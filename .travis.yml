language: node_js
node_js: "node"
branches:
  only:
    - master
    - main
cache:
  directories:
    - node_modules
services:
  - mongodb
before_script:
  - mongo mydb_test --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'
script:
  - npm run lint